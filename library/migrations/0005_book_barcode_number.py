# Generated by Django 4.2 on 2026-03-31 15:22

import random

from django.db import migrations, models


def populate_barcode_numbers(apps, schema_editor):
    Book = apps.get_model('library', 'Book')
    used = set(Book.objects.exclude(barcode_number__isnull=True).exclude(barcode_number='').values_list('barcode_number', flat=True))

    for book in Book.objects.all():
        if book.barcode_number:
            continue
        candidate = f"BK{random.randint(1000000000, 9999999999)}"
        while candidate in used:
            candidate = f"BK{random.randint(1000000000, 9999999999)}"
        used.add(candidate)
        book.barcode_number = candidate
        book.save(update_fields=['barcode_number'])


def noop_reverse(apps, schema_editor):
    pass


class Migration(migrations.Migration):

    dependencies = [
        ('library', '0004_bookrenewal'),
    ]

    operations = [
        migrations.AddField(
            model_name='book',
            name='barcode_number',
            field=models.CharField(blank=True, db_index=True, max_length=16, null=True, unique=True),
        ),
        migrations.RunPython(populate_barcode_numbers, noop_reverse),
    ]
